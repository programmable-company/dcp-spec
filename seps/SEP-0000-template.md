# SEP-0000 — <Title in title case>

| | |
| --- | --- |
| **SEP number** | 0000 (assigned by maintainer on acceptance into Discussion) |
| **Status** | Draft |
| **Type** | Standards / Process / Informational |
| **Authors** | Name `<email or handle>` |
| **Created** | YYYY-MM-DD |
| **Last updated** | YYYY-MM-DD |
| **Discussion** | <link to discussion thread; "n/a" while pre-alpha> |
| **Targets specification version** | <YYYY-MM-DD or "next"> |
| **Resolution** | <link to resolution; left blank until Accepted/Rejected> |

> *Delete this quote block before submitting. The bracketed instructions in each section are guidance — replace them with the SEP's content.*

## Abstract

> *One paragraph. What is being proposed, in language a reader unfamiliar with the SEP can understand.*

## Motivation

> *Why is this change needed? What problem does it solve? What goes wrong if the project does nothing?*
>
> *Cite specific places in the specification or schema that exhibit the problem. Link to the relevant specification sections (`specification/<DATE>/<file>.md`) and schema files (`schema/<DATE>/src/<file>.ts`).*

## Specification

> *What exactly changes? Be precise. If the change is to normative prose, quote the current text and propose the replacement. If the change is to schema types, show the TypeScript before-and-after. If the change is to a profile, name the profile and the section.*
>
> *RFC 2119 / BCP 14 keywords (`MUST`, `SHOULD`, `MAY`) should appear here as they will appear in the resulting specification, in all caps.*

### Schema impact

> *List every TypeScript type added, modified, or removed. Show the new type definitions in full.*

### Repository structure impact

> *Does this change affect the file/directory layout in `specification/<DATE>/02-repository-structure.md`? If so, how?*

### Migration impact

> *If existing repositories at the previous draft would no longer be conformant after this change, describe the migration path. If the change is forward-compatible only, say so.*

## Rationale

> *Why this design? What alternatives were considered, and why were they rejected?*
>
> *If the SEP touches an area covered by an existing standard (JCS, JWS, JAdES, JSON Schema, BCP 14), explain why this proposal does not violate or duplicate that standard.*

## Backwards compatibility

> *What existing repositories, tools, or implementations break? Quantify if possible.*
>
> *If nothing breaks, say "None."*

## Security considerations

> *What attack surface does this change open or close? What assumptions does the change make about producers, verifiers, key custody, or trust anchors?*
>
> *If the SEP has no security impact, state so explicitly. Do not omit this section.*

## Privacy considerations

> *What personal data, organizational data, or sensitive metadata does this change affect? In particular, the Estonia profile's `ee.isikukood` is GDPR-sensitive; SEPs touching identifier handling must address this.*
>
> *If the SEP has no privacy impact, state so explicitly.*

## Reference implementation

> *Optional. If the SEP comes with a reference implementation, link to it. Reference implementations are not required but accelerate review.*

## Discussion

> *Link to discussion venues — issue tracker, mailing list, recorded calls. Summarize unresolved threads inline so readers can find them without scrolling external history.*

## Resolution

> *Filled in when the SEP reaches Accepted or Rejected. Includes the rationale for the resolution and (for Accepted SEPs) a pointer to the dated draft in which the change lands.*
